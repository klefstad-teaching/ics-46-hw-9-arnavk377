#include "ladder.h"
#include <string>

int main() {
    // cout << edit_distance_within("bye", "byed", 1) << endl;
    // cout << edit_distance_within("baed", "byed", 1) << endl;
    // cout << edit_distance_within("bye", "by", 1) << endl;
    // cout << edit_distance_within("bye", "ye", 1) << endl;

    verify_word_ladder();

    return 0;
}